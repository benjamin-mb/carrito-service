server:
  port: 8084

notificacion:
  webhook:
    url: http://localhost:8080/webhook-mock

spring:
  application:
    name: carrito-service

  config:
    import: optional:configserver:http://localhost:8888
  profiles:
      active: dev

  rabbitmq:
    host: localhost
    port: 5672
    username: arka
    password: arka123